# BibleArchive (BAR)

A Rust library for reading and writing [BARFile format] Bible archives. The [BARFile format] is 
designed to be compact, but to still allow access to verses and chapters, on individual lookups
or on iterating over the whole text, at speed.

[BARFile format]: BARFileFormat.md
